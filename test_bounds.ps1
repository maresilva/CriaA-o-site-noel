 = Get-Content -Path portfolio.html -Raw
 = '<!-- MAIN CONTENT: SOLUCOES -->'
 = '</main>'
 = .IndexOf()
 = .IndexOf(, ) + .Length

if ( -ge 0 -and  -gt ) {
    Write-Host "Found bounds!"
} else {
    Write-Host "Bounds not found!"
}
